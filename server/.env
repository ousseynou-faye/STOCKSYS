# // server/.env.example
# Remplacer par vos secrets r�els avant d�ploiement.

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/stocksys?schema=public&sslmode=require
JWT_SECRET=change_me_with_a_strong_random_secret
PORT=3000

# Swagger : d�sactiver en prod ou prot�ger par auth/IP
SWAGGER_ENABLED=false
SWAGGER_USER= # optionnel (basic auth Swagger)
SWAGGER_PASSWORD= # optionnel (basic auth Swagger)
SWAGGER_IP_WHITELIST= # optionnel, liste d IP autorisees separees par des virgules

LOG_WEBHOOK_URL= # optionnel: URL pour remonter les logs (scope_violation, api_error) vers un aggregateur/alerting
